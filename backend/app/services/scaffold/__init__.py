"""Deterministic repo-scaffold helpers (tags, README, CI, .gitignore, tfvars, zip).

Everything here is pure/text — no LLM, no terraform binary — so it works on the
free-tier host and produces byte-stable output.
"""
